<UserControl x:Class="SSEL.MONTERREY.WPF.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SSEL.MONTERREY.WPF.ViewModels">

    <!-- DataContext -->
    <UserControl.DataContext>
        <vm:ConfiguracionVM/>
    </UserControl.DataContext>

    <!-- Contenedor principal -->
    <Grid Background="{DynamicResource FondoColor}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Configuración General del Sistema"
                       FontFamily="{DynamicResource FuenteTitulos}"
                       Foreground="{DynamicResource TextoPrimario}"
                       FontSize="22"
                       HorizontalAlignment="Center"/>

            <Border Background="White"
                    Padding="20"
                    CornerRadius="8"
                    BorderBrush="{DynamicResource BordeBrush}"
                    BorderThickness="1"
                    Width="400">

                <StackPanel>
                    <Label Content="Nombre del Sistema:"/>
                    <TextBox Text="{Binding NombreSistema}" />

                    <Label Content="Ruta de Archivos:"/>
                    <TextBox Text="{Binding RutaArchivos}" />

                    <Button Content="Guardar Configuración"
                            Command="{Binding GuardarCommand}"
                            HorizontalAlignment="Center"
                            Margin="10"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

